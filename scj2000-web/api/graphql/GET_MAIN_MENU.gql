query GET_MAIN_MENU {
    mainmenu {
        items {
        	menulinks_id(filter: {status: {_eq: "published"}}) {
                id
                title
                url
                status
                sort
            }
        }
    }
}